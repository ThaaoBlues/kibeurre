use crate::math_utils::{Vector,empty_vector};
use crate::parameters::{n, m};


/*
There, we co
*/
pub fn bytes_to_vector(input: &[u8; n]) -> Vector<256> {

    let mut tmp = empty_vector();
    for i in 0..n {
        
    }
    Vector::new(&tmp, 3329)
}